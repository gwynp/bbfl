<h1> BBFL Franchise List.</h1>


<table class="table table-striped table-bordered table-condensed">
  <thead>
    <tr>
      <th>Franchise</th>
      <th>Division</th>
      <th>Email</th>
    </tr>
  </thead>

  <tbody>
    <% @users11.each do |user| %>
      <tr>
        <td><%= user.franchise %></td>
        <td><%= user.division %></td>
        <td><%= user.email %></td>
       
            <%# link_to 'Change', edit_user_registration_path(user) %>
            <%# link_to 'Delete', user, method: :delete, data: { confirm: 'Are you sure?' } %>
       
      </tr>
    <% end %>
  </tbody>
</table>